import { generatePDF } from './server/pdfGenerator.ts';
import fs from 'fs';

const testData = {
  evidenceName: "اختبار النقاط في PDF",
  subEvidenceName: "اختبار تقني",
  description: `هذا نص عادي في البداية.

النقاط التالية يجب أن تظهر كقائمة:
- النقطة الأولى: اختبار النقاط
- النقطة الثانية: التأكد من التنسيق
- النقطة الثالثة: التحقق من المسافات

وهذا نص عادي بعد القائمة.`,
  userFieldsData: {},
  page2BoxesData: [
    {
      title: "الأهداف",
      content: `الأهداف الرئيسية:
- تحسين جودة التعليم
- رفع مستوى الطلاب
- تطوير المهارات

نص عادي بعد القائمة.`
    },
    {
      title: "الإجراءات",
      content: `تم تنفيذ الإجراءات التالية:
• إجراء أول
• إجراء ثاني
• إجراء ثالث`
    },
    {
      title: "النتائج",
      content: `نص عادي بدون نقاط.
هذا سطر آخر.
وسطر ثالث.`
    }
  ],
  selectedTheme: "white"
};

console.log('🧪 اختبار توليد PDF مع النقاط...');

try {
  const pdfBuffer = await generatePDF(testData);
  fs.writeFileSync('/home/ubuntu/test-bullets.pdf', pdfBuffer);
  console.log('✅ تم توليد PDF بنجاح!');
  console.log('📄 الملف: /home/ubuntu/test-bullets.pdf');
  console.log(`📊 الحجم: ${(pdfBuffer.length / 1024).toFixed(2)} KB`);
} catch (error) {
  console.error('❌ خطأ:', error.message);
}
