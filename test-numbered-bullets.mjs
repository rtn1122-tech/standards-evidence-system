import { generatePDF } from './server/pdfGenerator.ts';
import fs from 'fs';

const testData = {
  evidenceName: "اختبار النقاط المرقمة في PDF",
  subEvidenceName: "اختبار شامل",
  description: `مقدمة عادية.

النقاط المرقمة:
1. النقطة الأولى المرقمة
2. النقطة الثانية المرقمة
3. النقطة الثالثة المرقمة

نص عادي بعد القائمة المرقمة.

النقاط العادية:
- نقطة عادية أولى
- نقطة عادية ثانية

خاتمة.`,
  userFieldsData: {},
  page2BoxesData: [
    {
      title: "الأهداف",
      content: `الأهداف الرئيسية:
1. تحسين جودة التعليم
2. رفع مستوى الطلاب
3. تطوير المهارات
4. تعزيز الإبداع`
    },
    {
      title: "الإجراءات",
      content: `الإجراءات المنفذة:
1. عقد اجتماع تحضيري
2. توزيع المهام على الفريق
3. متابعة التنفيذ
4. تقييم النتائج
5. كتابة التقرير النهائي`
    },
    {
      title: "خليط من النقاط",
      content: `النقاط المرقمة:
1. أولاً
2. ثانياً
3. ثالثاً

النقاط العادية:
- نقطة
- نقطة أخرى

نص عادي في النهاية.`
    }
  ],
  selectedTheme: "white"
};

console.log('🧪 اختبار النقاط المرقمة في PDF...');

try {
  const pdfBuffer = await generatePDF(testData);
  fs.writeFileSync('/home/ubuntu/test-numbered.pdf', pdfBuffer);
  console.log('✅ تم توليد PDF بنجاح!');
  console.log('📄 الملف: /home/ubuntu/test-numbered.pdf');
  console.log(`📊 الحجم: ${(pdfBuffer.length / 1024).toFixed(2)} KB`);
} catch (error) {
  console.error('❌ خطأ:', error.message);
  console.error(error.stack);
}
